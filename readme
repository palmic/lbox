MetaRecords
-------------

- ... pokracovat testovanim editace existujicich nodes
 - nasledne zaridit moznost pokrocilych referenci recordu jako ref_image => {imageRecord} a ref_{boundedRecord} => select_label_colname